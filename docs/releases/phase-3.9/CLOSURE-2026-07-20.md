# Phase 3.9 engineering closure — 2026-07-20

## Status

Phase 3.9 is **engineering-complete**. The active daily-short-flow shadow is intentionally still collecting prospective evidence; monitoring is ongoing work, not an unfinished implementation. Phase 3.91 remains queued and has not started.

No score weights, thresholds, decision policy or live diagnostics were changed by this closure slice.

## Frozen release boundary

- Live model: `coverage-aware-renormalized-v4-finra-freshness-live`.
- Immediate rollback: `coverage-aware-renormalized-v3-live`.
- Active shadow: `technology-daily-short-flow-v3-shadow`.
- Current readiness: **Collecting evidence**, 1/6 gates green.
- Current active-shadow evidence: 178 included comparisons, 46 mature pairs across 23 dates, and 19 pending changed-diagnostic observations across six dates.

The current live and shadow utility curves overlap because no changed-diagnostic row has yet matured. This is an expected scientific state, not missing data. Refreshes can capture new immutable observations; only elapsed time and later market data can mature a three-month outcome.

## Delivered Phase 3.9 surface

- explicit, reversible coverage-aware model promotion and immutable archive;
- corrected FINRA freshness policy with a retained v3 rollback model;
- active daily short-flow-slope shadow hypothesis, isolated from live scores;
- self-explanatory Model tuning decision center, lineage, gates and maturity progress;
- prospective live monitoring against frozen promotion evidence;
- provider-health and scheduler operations without requiring an open browser;
- verified backups and isolated restore drills;
- research alerts, options-evidence monitoring and non-executing Scenario lab;
- responsive all-page UI regression coverage and real desktop/phone browser acceptance.

## Acceptance evidence

- `python -m pytest -q`: 218 passed; one upstream yfinance deprecation warning.
- `tests/test_ui_regression.py`: 17 passed.
- Python compilation passed for application, pages, source, scripts and tests.
- Real Chromium acceptance passed 16/16 route/viewport checks at desktop and phone sizes.
- Streamlit health returned `ok` on port 8501 after restart.
- Backup manifest verification and isolated restore drill passed without touching the live database.

## Runtime repair

The project-local virtual environment contained 15,448 macOS File Provider `dataless` dependency files, making heavy UI imports take roughly 284 seconds. The environment was rebuilt under local Application Support and `.venv` now points to it; the equivalent import takes under one second. The original environment is retained locally as `.venv-cloud-backup-20260720` and excluded from Git.

Rollback: stop the server, remove the `.venv` symlink and atomically rename the retained backup to `.venv`. No application data or schema migration is involved.

## Monitoring handoff

The first changed-decision outcomes should begin maturing around 2026-10-15. Review the full current cohort around 2026-10-20, then assess all six gates without reactive optimization. Do not begin Phase 3.91 or promote the shadow merely because time has elapsed; either action requires its own deliberate decision.

Release rollback remains commit/tag based, while model rollback remains the separately registered `coverage-aware-renormalized-v3-live` policy.
